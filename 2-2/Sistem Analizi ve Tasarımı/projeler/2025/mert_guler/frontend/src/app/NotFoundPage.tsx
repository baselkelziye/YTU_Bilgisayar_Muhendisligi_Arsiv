import React from 'react';

const NotFoundPage: React.FC = () => {
    document.title = `Not Found - Listopia`
    return (
        <div>
            Not Found
        </div>
    );
};

export default NotFoundPage;
